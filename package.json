{
  "name": "stockwise-insights",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontEnd",
    "backEnd"
  ],
  "scripts": {
    "build": "cd frontEnd && npm run build && cd .. && mkdir -p public && cp -r frontEnd/.output/public/* public/"
  },
  "dependencies": {
    "axios": "^1.20.0",
    "cors": "^2.8.6",
    "dotenv": "^18.0.3",
    "express": "^5.2.1",
    "mongoose": "^9.10.2"
  }
}
